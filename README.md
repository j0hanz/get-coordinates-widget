# Get-Coodinates Widget

## Style Variants

- **Default**: Renders the coordinate output with the widget surface background for maximum contrast and parity with the original design.
- **Linear**: Uses transparent backgrounds with a subtle bottom divider to blend into minimal interfaces and toolbars.

Select the preferred style in the widget settings under *Style variant*. The choice applies instantly in both builder and runtime modes.
